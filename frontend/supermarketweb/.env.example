## 前端环境变量示例（复制为 .env.development.local / .env.production.local 使用）
##
## 开发服务器端口（可选）
# VITE_PORT=8080
##
## 是否强制使用 Mock（true/false）
# VITE_USE_MOCK=false
##
## 当真实接口失败时，是否回退使用 Mock（true/false）
## 默认：true
# VITE_FALLBACK_MOCK=true

